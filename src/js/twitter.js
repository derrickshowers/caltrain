(function($) {
  'use strict';

  var $twitterContainer = $('.twitter-timeline');
  var $twitterHeading = $('#twitter-heading');

  /**
   * does just what you'd think it does
   * no return, no parmas, just init
   */
  function init() {

    var deviceHeight = $(window).height();

  }

  init();

})(jQuery);